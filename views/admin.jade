extends layout
block script
    script(src='/socket.io/socket.io.js')
    script(src='js/moment.min.js')
    script(src='js/jquery-1.8.3.min.js')
    script
        var socket = io.connect('http://localhost:5000');
        document.getElementById('elapsedTime').innerHTML = 
        socket.on('newgame', function(data) {
            document.getElementById('currGameCount').innerHTML = data.size;
            $('#currGameIdList').append('<li>' + data.game.id + '</li>');
        });    

block content 
    h1 Admin Console
    p Yay admin console :)
    h3 Number of Current Games: 
    - if (typeof(games) !== 'undefined'){
        #currGameCount #{games.length}
    - }
    h3 Games
    #currGameIdList 
    - if (typeof(games) !== 'undefined'){
        each game in games
            li= game.start
    - }